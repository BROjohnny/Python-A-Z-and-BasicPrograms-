import math

print(math.pi)
print(math.pow(2,3))
print(math.sin(math.pi/2))

import math as m #we assign m for math name

print(m.pi)
print(m.pow(2,3))
print(m.sin(m.pi/2))

from math import sqrt #we do not need call whole math library, just import sqrt part

print(sqrt(4)) # we do not need call as math.sqrt because we just call sqrt

